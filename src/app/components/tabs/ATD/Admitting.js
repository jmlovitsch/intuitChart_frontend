import React, { Component } from 'react'
import { connect } from 'react-redux'

class Admitting extends Component {
    render() {
        return (
            <div>
                Admitting
            </div>
        )
    }
}

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(Admitting)
